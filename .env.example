VITE_API_BASE_URL=http://localhost:8000
# token   = bearer token returned from /api/login (backend setup included)
# sanctum = cookie-based SPA auth (optional; needs different backend setup)
VITE_AUTH_MODE=token
